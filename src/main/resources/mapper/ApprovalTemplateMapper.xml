<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.workflow.mapper.ApprovalTemplateMapper">

    <select id="selectById" resultType="com.example.workflow.entity.WfApprovalTemplate">
        SELECT * FROM wf_approval_template WHERE id = #{id}
    </select>

    <select id="selectByProcessKey" resultType="com.example.workflow.entity.WfApprovalTemplate">
        SELECT * FROM wf_approval_template WHERE process_key = #{processKey} AND status = 1
        ORDER BY rule_type, created_at DESC
    </select>

    <select id="selectAll" resultType="com.example.workflow.entity.WfApprovalTemplate">
        SELECT * FROM wf_approval_template WHERE status = 1 ORDER BY created_at DESC
    </select>

    <insert id="insert">
        INSERT INTO wf_approval_template (id, process_key, template_name, rule_type, rule_config, status, created_by)
        VALUES (#{id}, #{processKey}, #{templateName}, #{ruleType}, #{ruleConfig}, #{status}, #{createdBy})
    </insert>

    <update id="update">
        UPDATE wf_approval_template SET
            template_name = #{templateName},
            rule_type = #{ruleType},
            rule_config = #{ruleConfig}
        WHERE id = #{id}
    </update>

    <update id="updateStatus">
        UPDATE wf_approval_template SET status = #{status} WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM wf_approval_template WHERE id = #{id}
    </delete>
</mapper>
